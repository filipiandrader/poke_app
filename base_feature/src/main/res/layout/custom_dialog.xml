<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/background_custom_dialog">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/customDialogMessageTextView"
        style="@style/PokeApp.TextAppearance.Label.Center"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="32dp"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="32dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Message Text" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/customDialogActionButton"
        style="@style/DexApp.Button.Primary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        app:layout_constraintEnd_toEndOf="@+id/customDialogMessageTextView"
        app:layout_constraintStart_toStartOf="@+id/customDialogMessageTextView"
        app:layout_constraintTop_toBottomOf="@+id/customDialogMessageTextView"
        tools:text="OK" />
</androidx.constraintlayout.widget.ConstraintLayout>